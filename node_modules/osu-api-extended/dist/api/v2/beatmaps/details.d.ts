import { BeamapsDetailsDifficulty } from "../../../types/v2/beamaps_details_difficulty";
import { BeamapsDetailsSet } from "../../../types/v2/beamaps_details_set";
type params = ({
    type: 'difficulty';
    id: number;
} | {
    type: 'set';
    id: number;
});
type Response<T extends params['type']> = T extends 'difficulty' ? BeamapsDetailsDifficulty : T extends 'set' ? BeamapsDetailsSet : never;
declare const name: <T extends params>(obj: T) => Promise<Response<T["type"]>>;
export default name;
