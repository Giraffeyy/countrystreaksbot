import { Modes_names } from "../../../../types";
import mods from '../../../../tools/mods';
import { ScoresUser } from '../../../../types/scores_user';
declare const name: (user_id: number, type: 'recent' | 'best' | 'firsts' | 'pinned', object?: {
    include_fails?: boolean;
    mode?: Modes_names;
    limit?: number;
    offset?: number;
    mods?: number;
}) => Promise<ScoresUser[]>;
export default name;
