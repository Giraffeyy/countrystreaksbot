import { Modes_names } from "../../../../types";
declare const name: (beatmap_id: number, user_id: number, obj?: {
    mode?: Modes_names;
    mods?: string[];
    best_only?: boolean;
}) => Promise<any>;
export default name;
