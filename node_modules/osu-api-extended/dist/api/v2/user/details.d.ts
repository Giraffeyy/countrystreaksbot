import { Modes_names } from "../../../types";
import { UserDetails } from '../../../types/user_details';
declare const name: ({ myself, user, mode, key }: {
    myself?: boolean;
    user?: number;
    mode?: Modes_names;
    key?: 'id' | 'username';
}) => Promise<UserDetails>;
export default name;
