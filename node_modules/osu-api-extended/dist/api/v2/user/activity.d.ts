import { UserDetails } from '../../../types/user_details';
declare const name: ({ user, limit, offset }: {
    user: number;
    limit: number;
    offset: string;
}) => Promise<UserDetails>;
export default name;
