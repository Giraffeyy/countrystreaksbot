export interface types {
    (difficulty_id: number, file_path: string, overwrite?: boolean): Promise<{
        type: 'created' | 'exists' | 'rate-limit';
        path?: string;
    }>;
}
declare const name: types;
export default name;
